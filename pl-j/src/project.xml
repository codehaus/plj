<project>
    <extend>${basedir}/../project.xml</extend>
    <pomVersion>3</pomVersion>
    <artifactId>pl-j</artifactId>
    <name>pl-j</name>
    <groupId>pl-j</groupId>
    <description>PL-J Tools subprojects contains general classes and blocks such as classloader implementations and JDBC utility classes. These classes are used in most of the block implementation subprojects.</description>
    <shortDescription>PL-J general classes to build blocks on.</shortDescription>
    <properties/>
</project>